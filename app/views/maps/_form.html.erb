<%= form_with(model: map, local: true) do |form| %>
  <% if map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(map.errors.count, "error") %> prohibited this map from being saved:</h2>

      <ul>
      <% map.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :map_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :map_description %>
  </div>

  <div class="field">
    <%= form.label :size_x %>
    <%= form.number_field :size_x, id: :size_x %>
  </div>

  <div class="field">
    <%= form.label :size_y %>
    <%= form.number_field :size_y, id: :size_y %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
